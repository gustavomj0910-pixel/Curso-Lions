let temperatura = 30;
let mensagem;

if (temperatura >= 35) {
    console.log ('Está muito quente.')
} else { // Essa parte do código que será executada, porque a temperatura não é maior ou igual a 35.
    console.log ('Está agradável.') // Então, essa será a mensagem impressa.
}